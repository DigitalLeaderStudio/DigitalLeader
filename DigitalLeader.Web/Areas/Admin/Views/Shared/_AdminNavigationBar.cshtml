<nav class="navbar navbar-fixed-top">
    <div class="header-wrapper">
        <div class="container">
            <div class="navbar-header">
                <div class="branding-container">
                    <div class="logo">
                        <a href="@Url.Action("Index", "Home",new { area = ""})"><img src="~/Content/images/logo.png" /></a>
                    </div>
                    @*<div class="site-name">@Html.ActionLink("Digital Leader", "Index", "Home", new { area = "" }, new { @class = "" })</div>*@
                    @*<div class="motto">your best digital solutions provider</div>*@
                    @*<div class="motto">Today you are reader<br /> tomorrow you are Leader!</div>*@
                </div>
                <button type="button" class="navbar-toggle" data-target="#mainNavLinks">
                    <i class="fa fa-bars" aria-hidden="true"></i>
                </button>
                <div class="main-cta">
                    <h1>
                        <a class="call-cta" href="callto:+38961212713">BACKEND</a>
                    </h1>
                    <h2>
                        <a class="message-cta" href="">For Team Only</a>
                    </h2>
                </div>
            </div>
        </div>
    </div>
    <div class="navigation-wrapper">
        <div class="container">
            <div class="" id="mainNavLinks">
                <ul class="navbar-links">
                    <li class="active"><a href="@Url.Action("Index", "Admin", new { area = "Admin"})"><i class="fa fa-home"></i></a></li>
					<li>
						<a href="@Url.Action("Index", "Slider", new { area = "Admin"})">Slider</a>
					</li>
                    <li>
                        <a href="@Url.Action("Index", "Blogpost", new { area = "Admin"})">Blog</a>
                    </li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Portfolio <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li>
                                <a href="@Url.Action("Index", "Project", new { area = "Admin"})">Projects</a>
                            </li>
                            <li>
                                <a href="@Url.Action("Index", "Testimonial", new { area = "Admin" })">Testimonials</a>
                            </li>
                        </ul>
                    </li>
                    <li>
                        <a href="@Url.Action("Index", "Client", new { area = "Admin"})">Clients</a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index", "User", new { area = "Admin"})"><i class="fa fa-users"></i> Users</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>
<script>
    $(document).ready(function () {
        $screenWidth = window.screen.width + 'px';
        $('.navigation-wrapper').css({ 'left': $screenWidth, 'display': 'block' });

        $('.navbar-toggle').on('click', function () {
            $sign = $(this).find("i");

            if ($sign.hasClass("fa-bars")) {
                $sign.removeClass("fa-bars");
                $sign.addClass("fa-times");
                $(".navigation-wrapper").animate({ 'left': '0' }, { duration: 300, easing: "swing" });
            }
            else {
                $sign.removeClass("fa-times");
                $sign.addClass("fa-bars");
                $width = window.screen.availWidth;
                $(".navigation-wrapper").animate({ 'left': $width }, { duration: 300, easing: "swing" });
            }
        });
    });
</script>